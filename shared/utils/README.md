# Utilities

Shared helpers used across workflows and modules.

- `progress.py`: elapsed-time logging.
- `bet_explain.py`: market/selection annotations for previews.
- `cli_presets.py`: recommended no-tune CLI argument presets (`punter go`).
